package coordinator;

public interface ComputationCoordinator {

	// Method to receive the user request, start computation, and handle data
	// storage interaction
//	void compute(String inputFilePath, char delimiter, String outputFilePath);

}
